import { FC } from "react";

interface Button {
    children,

};

const Button: React.FC<ButtonProps> = () => {
    return (
        <div>
            Button
        </div>
    );
};

export default Button;
